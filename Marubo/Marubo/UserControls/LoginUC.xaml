<UserControl x:Class="Marubo.UserControls.LoginUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Marubo.UserControls"
             xmlns:utils="clr-namespace:Marubo.Utils"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <AdornerDecorator Margin="0,10,0,0">
            <TextBox x:Name="TbEmail" Text="" Style="{StaticResource MaterialDesignTextBox}">
                <utils:WatermarkService.Watermark>
                    <TextBlock>Email</TextBlock>
                </utils:WatermarkService.Watermark>
            </TextBox>
        </AdornerDecorator>

        <AdornerDecorator Margin="0,10,0,0">
            <PasswordBox x:Name="PbPassword" Style="{StaticResource MaterialDesignPasswordBox}">
                <utils:WatermarkService.Watermark>
                    <TextBlock>Password</TextBlock>
                </utils:WatermarkService.Watermark>
            </PasswordBox>
        </AdornerDecorator>

        <Button x:Name="btnLogin" Content="Log ind" Style="{StaticResource MaterialDesignRaisedButton}" MinWidth="250" HorizontalAlignment="Center" Click="BtnLogin_Click" Margin="0,10,0,0"/>
        <Button x:Name="BtnBack" Content="Gå tilbage" Style="{StaticResource MaterialDesignRaisedButton}" MinWidth="250" HorizontalAlignment="Center" Click="BtnBack_Click" Margin="0,10,0,0"/>

    </StackPanel>
</UserControl>
